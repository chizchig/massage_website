// src/components/Booking/Confirmation.js
import React from 'react';

const Confirmation = ({ message }) => (
  <section>
    <h2>Booking Confirmation</h2>
    <p>{message}</p>
  </section>
);

export default Confirmation;
